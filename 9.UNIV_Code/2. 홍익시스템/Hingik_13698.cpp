#include<iostream>
#include<vector>
using namespace std;

int main()
{
	int s_ball=0, b_ball=3;
	int ptr = 0;
	char arr[200];
	vector<int> v;
	char tmp;
	while(tmp !='\n'){
		cin >> tmp;

		if (tmp != '\n')
			break;

		else {
			arr[ptr] = tmp;
			ptr += 1;
		}
	}

	for (int i = 0; i < ptr; i++)
	{
		switch (arr[i]) {
		case:
			break;

		case:
			break;
		}
	}

	


}