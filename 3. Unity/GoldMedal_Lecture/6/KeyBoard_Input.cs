using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeyBoard_Input : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        /*if (Input.anyKeyDown) //아무거나 눌러도 true -> bool 값이다.   
            Debug.Log("플레이어가 아무 키를 눌렀습니다.");

        // 키보드의 조작 방식은 꾹 누루고 있는 것이 있고,
        // 눌렀다가 떼는 방식이 있고,
        // 눌렀다가 올라갈때 입력 되는 3가지 방식이 존재한다.

        if (Input.GetKeyDown(KeyCode.Return)) //리턴은 엔터키이다.
            Debug.Log("아이템을 구입하였습니다.");

        if (Input.GetKey(KeyCode.LeftArrow))
        {
            Debug.Log("왼쪽으로 이동 중");
        }

        if (Input.GetKeyUp(KeyCode.RightArrow))
        {
            Debug.Log("오른쪽 이동을 멈추었습니다.");
        }

        if (Input.GetMouseButtonDown(0)) // 0은 마우스 왼쪽 버트, 1은 오른쪽
            Debug.Log("미사일 발사");

        if (Input.GetMouseButton(0))
            Debug.Log("미사일 모으는 중 ");

        if (Input.GetMouseButtonUp(0))
            Debug.Log("슈퍼미사일 발사!!");

        if (Input.GetButtonDown("Jump"))
            Debug.Log("점프했다.");

        if (Input.GetButton("Horizontal"))
            Debug.Log("횡 이동중 " + Input.GetAxis("Horizontal"));*/
    } 
}
